import { buildGlideDemo } from './symmetry-elements-'
import type { BuildOptions, EntryModel } from '../kit'

export default function build(options: BuildOptions): EntryModel {
  return buildGlideDemo(options)
}
